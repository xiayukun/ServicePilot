<Application x:Class="ServicePilot.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:ServicePilot"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Dark" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>

            <!-- 可滚动的托盘一级菜单。
                 WPF-UI 的 ContextMenu 默认模板用裸 StackPanel 做 items host，没有 ScrollViewer，
                 项目过多时会被裁剪且无法滚动。这里复刻 WPF-UI 4.x 的 UiContextMenu 模板（同样的阴影/
                 圆角/内边距，以及 ContextMenuBackground / ContextMenuBorderBrush 主题资源——它们与子菜单
                 使用的 FlyoutBackground / FlyoutBorderBrush 指向同一底层颜色），仅把 items host 包进一个
                 带 MaxHeight 的 PassiveScrollViewer，从而所有层级视觉统一且都可滚动。 -->
            <Style x:Key="ScrollableContextMenuStyle" TargetType="ContextMenu"
                   BasedOn="{StaticResource {x:Type ContextMenu}}">
                <Setter Property="Background" Value="{DynamicResource ContextMenuBackground}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource ContextMenuBorderBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource ContextMenuForeground}"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Grid.IsSharedSizeScope" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ContextMenu">
                            <ui:EffectThicknessDecorator Thickness="30">
                                <Border x:Name="Border"
                                        Padding="0,3,0,3"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="20" Direction="270"
                                                          Opacity="0.135" ShadowDepth="10" Color="#202020"/>
                                    </Border.Effect>
                                    <!-- DockPanel: footer 固定在底部（新增/管理/状态/退出 等），
                                         仅上方 items host（服务列表）随内容超高时滚动。 -->
                                    <DockPanel LastChildFill="True">
                                        <ContentPresenter DockPanel.Dock="Bottom"
                                                          Content="{Binding Path=(local:TrayContextMenu.Footer), RelativeSource={RelativeSource TemplatedParent}}"/>
                                        <ui:PassiveScrollViewer CanContentScroll="False"
                                                                MaxHeight="{TemplateBinding MaxHeight}"
                                                                HorizontalScrollBarVisibility="Disabled"
                                                                VerticalScrollBarVisibility="Auto">
                                            <StackPanel ClipToBounds="True"
                                                        IsItemsHost="True"
                                                        KeyboardNavigation.DirectionalNavigation="Cycle"
                                                        Orientation="Vertical"/>
                                        </ui:PassiveScrollViewer>
                                    </DockPanel>
                                </Border>
                            </ui:EffectThicknessDecorator>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 托盘菜单 MenuItem 样式：系统主题色 hover + 左侧选中条 -->
            <Style TargetType="ui:MenuItem" BasedOn="{StaticResource {x:Type ui:MenuItem}}">
                <Setter Property="Background" Value="Transparent"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <!-- DynamicResource (not ThemeResource): ThemeResource is a STATIC reference baked
                             at XAML load time, so it keeps WPF-UI's default blue even after we apply the
                             real Windows accent. DynamicResource re-resolves after ApplySystemAccent. -->
                        <Setter Property="Background" Value="{DynamicResource SystemAccentColorPrimaryBrush}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource SystemAccentColorPrimaryBrush}"/>
                        <Setter Property="BorderThickness" Value="3,0,0,0"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- ListBoxItem 选中样式：3px 左侧边框条 + 系统主题色，无全背景覆盖 -->
            <Style TargetType="ListBoxItem">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0,0,0,0"/>
                <Setter Property="Padding" Value="6,4"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListBoxItem">
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="{TemplateBinding Padding}"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#12FFFFFF"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource SystemAccentColorPrimaryBrush}"/>
                                    <Setter TargetName="Bd" Property="BorderThickness" Value="3,0,0,0"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                                    <Setter TargetName="Bd" Property="Background" Value="#30FFFFFF"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="#FFD4D4D4"/>
            </Style>
            <Style TargetType="Label">
                <Setter Property="Foreground" Value="#FFD4D4D4"/>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
